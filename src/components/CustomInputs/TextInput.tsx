import { TextInputParams } from "@/types";
import React from "react";

const TextInput = ({
  value,
  onChange,
  placeholder,
  clear,
  notEmpty,
}: TextInputParams) => {
  return <div></div>;
};

export default TextInput;
